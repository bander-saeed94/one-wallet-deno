export const sendEmail = async (
  receiverEmail: string,
  title: string,
  text: string,
) => {
  //todo
  console.log(`
  sendEmail():
  receiverEmail: ${receiverEmail},
  title: ${title},
  text: ${text},
  `);
};
